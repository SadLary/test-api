## Стек

- **React 18** + **TypeScript 5**
- **Zustand 4** — глобальное состояние
- **Vite** — сборщик
- **CSS Modules** — стили
- **Vitest** + **Testing Library** — тесты

## Быстрый старт

```bash
npm install
npm run dev
```